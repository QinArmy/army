package io.army.dialect;

public enum Clause {

    SELECT,
    SELECT_LIST,
    INSERT_INTO,
    VALUE,
    VALUES,
    FROM,
    ON,
    WHERE,
    GROUP_BY,
    HAVING,
    ORDER_BY,
    LIMIT,
    LOCK,
    SUB_QUERY,
    END
}
